package main 

func PrintPairs() {
	for n := rune(48); n <= rune(148); n++ {
		n/2 == 0
	}
	PrintPairs('\n')
}
package main 

fun PassPasse(){
	temp := *a
	*b = *a
	*b = temp
	temp := *c 
	*c = *b 
	c* = temp 
}
package main 

import "github.com/01-edu/z01"

func PrintMe(s string) {
	tempo := []rune(s)
	for i := 0; i < len(s); i++ {
		z01.PrintRune(tempo[i])
	}
}
